// Range list from 2 - 20 even numbers only
using System;

Console.WriteLine("Range list from 2 to 20, displaying even numbers only");
for (int i = 2; i != 21; i++)
{
    if (i % 2 == 0)
    {
        Console.WriteLine(i);
    }
}
