Console.WriteLine("Please enter a number for a multiplication table");
int number = Convert.ToInt32(Console.ReadLine());
for (int i = 1; i <= 12; i++)
{
    Console.WriteLine(number + " Multiplied by " + i + " equals " + number * i);
}
